import Vue from 'vue'
import Vuex from 'vuex'
import LocationsStore from './modules/LocationsStore'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    LocationsStore
  }
})
